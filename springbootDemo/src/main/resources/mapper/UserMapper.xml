<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="org.examplorfotg.springbootdemo.mapper.UserMapper">
    <select id="listAll" resultType="org.examplorfotg.springbootdemo.entity.User">
        select * from user
    </select>

    <!-- 通用查询映射结果 -->
    <resultMap id="BaseResultMap" type="org.examplorfotg.springbootdemo.entity.User">
        <id column="id" property="id" />
        <result column="no" property="no" />
        <result column="name" property="name" />
        <result column="password" property="password" />
        <result column="age" property="age" />
        <result column="sex" property="sex" />
        <result column="phone" property="phone" />
        <result column="role_id" property="roleId" />
        <result column="isValid" property="isvalid" />
    </resultMap>

    <!-- 通用查询结果列 -->
    <sql id="Base_Column_List">
        id, no, name, password, age, sex, phone, role_id, isValid
    </sql>

    <select id="pageC" resultType="org.examplorfotg.springbootdemo.entity.User">
        select * from user
    </select>

    <select id="pageCC" resultType="org.examplorfotg.springbootdemo.entity.User">
        select * from user ${ew.customSqlSegment}
    </select>
</mapper>
